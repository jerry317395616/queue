<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.business.client.mapper.ClientMapper">


    <select id="getRoomList" resultType="java.util.Map">
        select room.room_number, su.realname, dd.status, su.id doctorid,room.id roomid
        from rooms room
                 left join doctor_device dd on dd.room = room.id
                 left join sys_user su on su.id = dd.doctor
        where room.department = #{deptId}
    </select>
</mapper>